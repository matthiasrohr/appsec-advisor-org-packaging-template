# Custom threat actors for Acme Corp. Add, edit, or delete entries as needed.
# Each actor extends the plugin's default actor library for threat model analysis.
# Schema reference: https://github.com/matthiasrohr/appsec-advisor/blob/main/docs/org-profiles.md
actors:
  - id: ACT-E-1
    label: acme-privileged-contractor
    access:
      - internal-network
      - ci-cd-secrets
      - staging-env
    capabilities:
      sophistication: medium
      dwell_time: weeks
      surface_reach:
        - local
        - lateral
    motivation: financial
    heatmap_slug: repo-read
    description: External contractor with temporary elevated access.
